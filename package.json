{
  "scripts": {
    "compile": "rm -rf ./build && tsc",
    "document": "rm -rf ./doc && typedoc --out ./docs ./src && touch ./docs/.nojekyll"
  },
  "dependencies": {
    "@types/node": "^7.0.12",
    "tslib": "^1.6.0",
    "tslint": "^5.1.0",
    "typedoc": "^0.5.10",
    "typescript": "^2.2.2"
  }
}
